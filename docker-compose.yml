version: '3.9'

services:
  web:
    build: .
    container_name: drf_sqlite_app
    volumes:
      - .:/app
      - sqlite_data:/app/db
    ports:
      - "8000:8000"

volumes:
  sqlite_data: